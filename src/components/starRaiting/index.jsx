import {FaStar} from 'react-icons/fa';


export default function StarRating(){
    return <div className="star-raiting">
        {
            [...Array(noofStars)].map((_,index)=>{
                return 
            })
        }
    </div>
}